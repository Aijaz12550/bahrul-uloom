import { Grid } from "@material-ui/core";
import { FC, memo } from "react";

const SignUp: FC <any> = () => {
    return(
        <Grid>
            Sign Up
        </Grid>
    )
}

export default memo(SignUp);