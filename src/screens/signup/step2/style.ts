import { makeStyles } from "@material-ui/core";

export const styles = makeStyles((theme:any)=>({
    root:{
        width:"100%"
    },
    input:{
        width:"100%",
        marginBottom:"15px"
    }
}))