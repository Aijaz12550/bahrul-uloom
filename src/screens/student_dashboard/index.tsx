import { Grid, Typography } from "@material-ui/core"
import { FC } from "react"

const StudentDashboard: FC<any> = () => {
    return(
        <Grid container >
            <Typography>
                Student Dashboard
            </Typography>
        </Grid>
    )
}